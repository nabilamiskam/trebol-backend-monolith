Legacy callers of the `products` legacy path (/data/products)
=============================================================

This document lists files that reference the legacy `ProductsCrudServiceImpl` or the `/data/products` endpoint.

- `src/main/java/org/trebol/jpa/services/crud/impl/ProductsCrudServiceImpl.java` — legacy CRUD implementation; uses `ProductLookupService` for reads (ACL bridge).
- `src/main/java/org/trebol/api/controllers/DataProductsController.java` — controller exposing `/data/products` routes.
- `src/test/java/org/trebol/api/controllers/DataProductsControllerAclTest.java` — controller test that imports `ProductsCrudServiceImpl` to validate ACL-backed behavior.
- `src/test/java/org/trebol/api/controllers/DataProductsControllerTest.java` — controller unit tests for legacy endpoints.
- `src/test/java/org/trebol/jpa/services/crud/impl/ProductsCrudServiceImplTest.java` — unit tests for the CRUD service implementation.
- `src/test/java/org/trebol/jpa/services/crud/impl/ProductsCrudServiceImplAclTest.java` — tests covering ACL integration points.
- `src/test/java/org/trebol/jpa/services/crud/impl/ProductCreateImageRollbackTest.java` — integration test that instantiates `ProductsCrudServiceImpl`.
- `src/test/java/org/trebol/BackendAppTest.java` — application-level test referencing `DataProductsController`.
- `src/main/resources/application.properties` — security/CORS rules mentioning `/data/products` paths.

Notes and next steps
--------------------
- The main migration targets are callers that perform `readOne`/`getExisting`/create flows via `ProductsCrudServiceImpl` — these are the places where the ACL bridge is actively used.
- Next recommended action: produce a per-file call-site summary (lines and method names) so each caller can be migrated individually.

Generated by automated code inspection on 2026-06-11.
